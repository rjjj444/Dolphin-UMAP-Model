import sys
import subprocess

print(f"Installing to: {sys.executable}")
result = subprocess.run([sys.executable, "-m", "pip", "install", "hdbscan"], 
                       capture_output=True, text=True)
print(result.stdout)
print(result.stderr)
